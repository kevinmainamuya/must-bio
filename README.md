# must-bio
The simplest version of that domain is a single static HTML file hosted on GitHub Pages (free, HTTPS, instant setup). It needs no server, no backend — it is just a one-page WebAuthn handler that accepts parameters, runs the scan, and returns the credential.
